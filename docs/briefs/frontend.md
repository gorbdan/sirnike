# Брифы: фронтенд проекта

Формат: `- [ ] P0/P1/P2 · задача · критерий готовности`. Выполнил — `[x]` + строка в RELEASE_NOTES.md.
Зона роли — весь фронтенд (вебапп целиком + веб-страницы проекта), см. docs/roles/frontend.md.
⚠️ Сессия работает в репо вебаппа; контракт — docs/BOT_CONTRACT.md в репо бота.

## Очередь

- [ ] P0 · Поиск/фильтр шлёт неверные `cat_idx`/`item_idx` в payload «Использовать»:
      индексы, похоже, считаются по отфильтрованному списку, а бот резолвит их по
      полному каталогу — юзер выбирает «Полароид: детское фото», а бот отвечает
      «Стиль «Портреты»» с подсказкой «фото партнёра» (живой аудит бота
      audits/2026-07-07_live_ux_audit.txt, №3). Индексы должны всегда быть
      позициями в ПОЛНОМ prompt_library.json, независимо от активного поиска,
      фильтра и категории. Бот с 2026-07-07 игнорирует индексы, если prompt по
      ним не совпадает с присланным (защита), но title/hint при этом теряются —
      нужно чинить источник. Критерий: «Использовать» из результатов поиска
      называет тот же стиль, что открыт в карточке, и его же upload_hint.
- [ ] P1 · 1-клик до библиотеки с ИНЛАЙН-кнопок бота: поддержать
      `answerWebAppQuery` (вебапп получает query_id в initData → шлёт выбор через
      Telegram.WebApp API, а не sendData). Контекст: sendData работает только с
      reply-клавиатуры; попытка повесить web_app на инлайн-кнопки в боте молча
      ломала «Использовать» (audits/2026-07-07_live_ux_audit.txt, №1) и откачена.
      Критерий: «Использовать» доставляет выбор боту из вебаппа, открытого с
      инлайн-кнопки. Согласовать формат с бэкендом (нужен хэндлер в боте).
- [ ] P1 · Перед «Использовать» показывать «что загрузить» как обязательный шаг,
      если у стиля есть `upload_hint` (например, «Фото сейчас + детское фото»).
      Критерий: юзер не может применить стиль, не увидев требования к фото.
- [ ] P1 · Брендовые примеры вместо внешних заглушек (unsplash) в карточках
      (пункт P3 маркетинг-плана). Источник примеров: библиотека before/after
      от контент-продюсера (docs/briefs/content.md, задача P0).
- [ ] P2 · Раздел «Топ-стили» на главном экране каталога (по статистике использования
      шаблонов из бота — данные спросить у бэкенда).

## Выполнено

- [x] P0 · Ни одна карточка не приводит к «Шаблон «шаблон»» в боте. — 2026-07-02,
      ветка `fix/payload-title` в репо вебаппа. ⚠️ Итоговое решение НЕ как в исходной
      формулировке: `title` у фото-стилей НЕ заполняется (Аня удалила намеренно,
      не дописывать). Вместо этого payload всегда несёт `cat_idx`/`item_idx`
      (бот резолвит label сам, фикс в боте от 2026-07-02) и название категории
      как title-фолбэк; заглушка «Шаблон» убрана из app.js.
      Ждёт мержа + деплоя Cloudflare Pages (Аня); бот подтянет JSON при рестарте.
